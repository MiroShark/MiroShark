# Testing Rule

- Run tests after every code change
- Fix test failures before moving on — never leave broken tests
- Never comment out or skip failing tests to make them pass
- New features need tests; bug fixes need regression tests
- If no test framework exists, note it and suggest adding one
